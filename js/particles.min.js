/*!
 * A lightweight JavaScript library for creating particles.
 * @preserve
 */
!function (a) {
    "use strict";

    var b = a.document,
        c = a.Math;

    var d = function (a, b) {
        return a.x = b.x, a.y = b.y, a;
    };

    function e(a, b) {
        var c, d, e;
        return null == b ? b = a : (c = function (a, b) {
            return parseInt(a, b || 10);
        }(b)), d = isNaN(c), b.toString(e || 10), d;
    }

    // Continue com o restante do código da biblioteca aqui...

}(window);
